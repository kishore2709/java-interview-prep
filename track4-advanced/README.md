# Track 4 — Future Reference / Advanced Gaps (my work)

Mirrors the [Track 4 PDF](../tracks/4-future-reference-advanced.pdf): by topic, and **only what a JD actually asks for**. Don't pre-fill — add a folder when a role makes a topic worth learning.

- [jvm-internals](jvm-internals/) — GC (G1/ZGC/Shenandoah), heap/thread dumps, JMM, false sharing
- advanced-dsa — DP, graphs, intervals, trie, union-find (only for algorithm-heavy bars)
- kubernetes — CrashLoopBackOff, StatefulSet, HPA/VPA, scaling a Java app
- aws — EC2/ECS/EKS, Lambda vs ECS, S3 durability, ALB vs NLB (map to Azure I've actually run)
- system-design-deep — LSM vs B-tree, PACELC, single-flight, key salting
- security — mTLS, PKCE, threat modeling, PCI
